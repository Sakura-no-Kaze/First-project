import tkinter as tk

root = tk.Tk()
root.title("My First GUI")
root.geometry("300x200")

label = tk.Label(root, text="Hi!")
label.pack(pady=20)

button = tk.Button(root, text="Click to Exit", command=root.quit)
button.pack()

root.mainloop()
