import tkinter as tk

root = tk.Tk()
root.title("My First GUI")
root.geometry("3000x2000")

label = tk.Label(root, text="Hello, press the button to exit!", font=("Bold", 20))
label.pack(pady=20)

button1 = tk.Button(root, text="Click to Exit", command=root.quit)
button1.pack()

root.mainloop()
