print ("This is a stopwatch, and it will stop when you enter 'Stop'.")
import time
starting_time = time.time()
while True:
    if input() == 'Stop':
        break
    else:
        passed_time = time.time() - starting_time
        print("Current timeelapsed: " + str(passed_time) + "Seconds")