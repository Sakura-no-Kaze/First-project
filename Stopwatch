print ("This is a Stopwatch, and it will stop when you enter 'Stop please'.")
import time as Tm
starting_time = Tm.time()
while True:
    if input() == 'Stop please':
        break
    else:
        passed_time = Tm.time() - starting_time
        print("Current timeelapsed: " + str(passed_time) + "Seconds")