print("This is a Stopwatch, it values politeness, and it will stop when you enter 'Stop please'.")
import time as Tm

starting_time = Tm.time()

while True:
    if input() == 'Stop please':
        passed_time = Tm.time() - starting_time
        print("Timeelapsed: " + str(passed_time) + " Seconds")
        break